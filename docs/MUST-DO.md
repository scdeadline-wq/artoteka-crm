# MUST DO — отложено осознанно (решение от 2026-06-10)

## Секреты (отложено по решению Алексея 2026-06-10 — «пока оставь»)

1. **Сменить прод-пароли Postgres и MinIO** — текущие закоммичены в `deploy/docker-compose.vps.yml:56,69`
   и навсегда остались в истории git. Перенести значения в `deploy/.env` на сервере,
   из compose читать через `${...}`. После смены — `git filter-repo` для чистки истории (опционально).
2. **Сменить пароль owner-аккаунта** (`paruer@artoteka.ru / artoteka2026`) — лежит в `backend/app/seed.py:80`.
3. **JWT_SECRET**: убедиться, что в `.env` на сервере задан свой; добавить fail-fast в `config.py`,
   если значение равно `change-me-in-production`.

## HTTPS (блокер — нужен домен, вопрос к клиенту)

4. Let's Encrypt не выдаёт обычные сертификаты на голый IP. Нужно **купить/назначить домен**
   (например, crm.artoteka.ru) → nginx + certbot (конфиг `deploy/nginx.conf` уже есть),
   закрыть порт 8000 наружу, обновить `NEXT_PUBLIC_API_URL` и CORS.
   Это же разблокирует Telegram Mini App (F-17).

## Безопасность — требует решения клиента

5. **Разграничение ролей на запись**: сейчас любой залогиненный может править работы/клиентов/продажи.
   AI-батчи и Airtable-импорт закрыты под admin (сделано 2026-06-10), остальное — спросить клиента,
   что разрешено менеджеру.
6. **`/images/{path}` раздаёт бакет анонимно** (фото комнат клиентов, PDF экспертиз).
   Браузерные `<img>` не шлют токен — нужны подписанные URL (presigned S3) или cookie-auth. Не однострочник.

## Техдолг (не блокирует демо)

7. Вынос тяжёлых синхронных вызовов из event loop доделать по результатам нагрузки
   (weasyprint/smart_crop/boto3 — частично обёрнуто в to_thread 2026-06-10).
8. Инвентарный номер на sequence Postgres вместо max+1 (сейчас — ретрай на IntegrityError).
9. `types.ts`: нормализовать Decimal-поля (бэк отдаёт строки) вместо точечных `Number()`.
10. logrotate для `/var/log/artoteka-autopull.log` (сейчас — самотруncate в autopull.sh).
11. CORS: добавить боевой домен, когда появится (п.4).
